import LoginButton from "./LoginButton";
import ThemeButton from "./ThemeButton";
const Header = () => {
  return (
    <div>
      <ThemeButton />
      <LoginButton />
    </div>
  );
};

export default Header;
